<div class="navbar">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a {{action goHome}} class="brand"> YourJargon </a>

      <form class="navbar-search pull-left">
        <input type="text" class="search-query" placeholder="Search">
      </form>

      <ul class="nav pull-left">
        <li> <button {{action newTerm}} class="btn pull-right"> New Term </button></li>
      </ul>
      <ul class="nav pull-right">
        {{#if YJ.currentUser.id}}
        <li>
          <ul class="nav">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><b class="caret"></b>Account</a>
              <ul class="dropdown-menu">
                <li><a {{action toEdit}}> Change Settings </a></li>
                <li><a {{action logout}}>logout</a></li>
              </ul>
            </li>
          </ul>
        </li>
        {{else}}
        <li><a {{action toLogin}}>login</a></li>
        {{/if}}

      </ul>
    </div>
  </div>
</div>
