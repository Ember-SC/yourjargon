YJ.OrganizationProfileRoute = Ember.Route.extend(

     setupController: (controller) ->
       # organizationController = controller.get('organizationController')
       # organization = organizationController.get('content')
       # controller.get('applicationController').connectOutlet('organizationProfile', organization)
)