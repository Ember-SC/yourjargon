YJ.UserEditRoute = Ember.Route.extend(
 # route: '/edit'
 # #EVENTS
 # update: ((controller, event) ->
 #   controller.get('userController').get('transaction').commit()
 #   controller.send('goHome')
 # )

 setupController: (controller, model) ->
   # appController = controller.get('applicationController')
   # userController = controller.get('userController')
   # appController.connectOutlet(
   #   viewClass: YJ.EditUserView,
   #   controller: userController,
   #   context: YJ.currentUser
   # )
)