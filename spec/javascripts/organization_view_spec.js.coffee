#describe "Organization view", ->
#
#  beforeEach ->
#    @testUser = YJ.createUser('test user', 'test@example.com')
#
#  it "can create an organization", ->
#    view = YJ.OrganizationCreateView.create();
#
#  describe "an organization", ->
#
#    beforeEach ->
#      @org = @testUser.createOrganization('Organization Name')
#
#    it "can see the current organization by name", ->
#
#    it "can see the list of organizations that this user belongs to", ->
#
#    it "can change the name of the organization", ->
#
#    it "can delete the organization", ->
#
#    it "can invite others to join the organization", ->




